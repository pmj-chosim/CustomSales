package com.example.customsale.repository;

import com.example.customsale.repository.entity.ImageReviewHistory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ImageReviewHistoryRepository extends JpaRepository<ImageReviewHistory, Integer> {
}
